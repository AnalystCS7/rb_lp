<?xml version="1.0" encoding="UTF-8"?>
<odoo>
  <data>
    <!-- Define the view for the reward form -->
    <record model="ir.ui.view" id="royal_rewards.reward_form_view">
      <field name="name">royal_rewards.reward.form</field>
      <field name="model">royal_rewards.reward</field>
      <field name="inherit_id" ref="product.product_template_form_view"/>
      <field name="arch" type="xml">
        <xpath expr="//form/sheet/notebook/page[@name='variants']" position="after">
          <group name="royal_rewards" string="Royal Rewards">
            <field name="royal_rewards_points" string="Points" attrs="{'readonly': [('product_variant_count', '>', 0)]}"/>
            <field name="royal_rewards_active" string="Active" attrs="{'readonly': [('product_variant_count', '>', 0)]}"/>
          </group>
        </xpath>
      </field>
    </record>

    <!-- Define the view for the reward list -->
    <record model="ir.ui.view" id="royal_rewards.reward_tree_view">
      <field name="name">royal_rewards.reward.tree</field>
      <field name="model">royal_rewards.reward</field>
      <field name="inherit_id" ref="product.product_template_tree_view"/>
      <field name="arch" type="xml">
        <xpath expr="//tree/field[@name='product_variant_count']" position="
